/**
 * Sin hacer
 * @param words
 * @returns
 */
export function meshArray(words:string[]):string{
    const result:string = '';

    for (let i = 0; i < words.length - 1; i++){
    }
    return result;
}
